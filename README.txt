azp.BBCodeEditor

A BBCode Editor using dojo/dijit.

Simply dojo.require('azp.BBCodeEditor') it into your page and mark a textarea with an attribute of dojoType="azp.BBCodeEditor".
It will degrade to a normal textarea if javascript is turned off.

Use it, fork it, poke fun at it, whatever.

Luis
